# Code review triage — round 1

All 12 findings and both doc recommendations evaluated on merits. Accepted: all, with two
scope decisions recorded below. Nothing silently dropped. CODE_CHANGED pending the fix wave.

| #   | Decision          | Notes                                                                                                                                                                                                                                                                                                                                     |
| --- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | Accept            | Spec still requires persist+notify of a generation-time clamp; the fix keeps that behavior but makes preflight side-effect-free and re-loads the latest config post-generation-token-check, persisting the clamped level only if model+stored level still match the snapshot, merged into the LATEST config. Regression test required.    |
| S2  | Accept            | Bounded refresh helper (15s, mirroring Pi's own selector) shared by generation, setters, menu entry, and Save; on timeout: release pending, warn, continue with cached registry snapshot where defensible or fail the gate. Never-resolving-refresh tests.                                                                                |
| S3  | Accept            | `modules.catch(() => {})` guard at creation; `.catch` on background-task chains; move pre-try host calls inside try.                                                                                                                                                                                                                      |
| S4  | Accept            | `normalizeRecapText` maps `\n`/`\t` to spaces and strips C0/C1 controls (`\x00-\x1f`, `\x7f`, `\x9b`); test-trim cases.                                                                                                                                                                                                                   |
| S5  | Accept            | `timeoutMs: 60000` + modest `maxRetries` in completion options; manual `/recap` while pending notifies "Recap: a refresh is already in progress." Abort-on-input and shutdown-abort: logged, not fixed (risk of cancelling legitimate manual runs; inherited posture).                                                                    |
| S6  | Accept (narrowed) | Unique tmp filename (pid+random) and read-immediately-before-write. The reviewer's alternative (clamp in memory only, no background writes) is REJECTED: the spec explicitly requires persisting the generation-time clamp. S1's conditional persist already minimizes background writes.                                                 |
| S7  | Accept            | Type-guard the version value (non-string → "0.0.0" degrade path); test-baseline case. Reviewer's own uncertainty (trigger population may be empty) noted; cost is one line.                                                                                                                                                               |
| S8  | Accept (adjusted) | Notify empty conversation for manual, startup, and compaction; auto stays silent (spam avoidance; usually deduped anyway). Spec's retained-behavior bullet is unqualified and base behavior notified unconditionally; the source plan's "manual-only" decision loses to the spec per the precedence rule. test-index updated accordingly. |
| S9  | Accept            | Deferrable-completion harness seam; tests: input/shutdown/leaf-change during in-flight recap, double manual dispatch, shutdown teardown (timer count 0, no-op events), non-idle tick reschedule.                                                                                                                                          |
| S10 | Accept            | Three temp-dir cases: ENOENT → succeeds from `{}`; invalid JSON → throws refusal, file byte-identical; array root → refusal.                                                                                                                                                                                                              |
| S11 | Accept            | Consolidate `errorMessage`, one positive-safe-int parser, one `modelLabel` in config.ts; align settings-menu clamp notice to "Recap Thinking Level".                                                                                                                                                                                      |
| S12 | Accept            | Extract `scripts/test-support.ts` (model/response builders, FakeRegistry, canonical default config); keep test-config's independent spec-default derivation.                                                                                                                                                                              |
| D1  | Accept            | README "Breaking changes in 0.5.0" section (flags, legacy subcommands, `effort`, normalization).                                                                                                                                                                                                                                          |
| D2  | Accept            | AGENTS.md count-free phrasing.                                                                                                                                                                                                                                                                                                            |

Fix-task complexity: high (multi-module hardening + test work, but every fix pinned by the
findings) → codex gpt-5.6-sol, xhigh effort, in the integration worktree.
